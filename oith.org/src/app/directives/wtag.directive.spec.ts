import { WTagDirective } from './wtag.directive';

describe('WTagDirective', () => {
  it('should create an instance', () => {
    const directive = new WTagDirective();
    expect(directive).toBeTruthy();
  });
});
